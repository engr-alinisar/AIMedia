# Railway monorepo config.
# Each service must set Root Directory = "backend" in Railway dashboard.
# API service uses:    backend/railway.toml     (dockerfilePath = src/AiMedia.API/Dockerfile)
# Worker service uses: Railway UI override      (dockerfilePath = src/AiMedia.Worker/Dockerfile)
